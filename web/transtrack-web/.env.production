# Not a secret — just the public URL the browser calls. Committed so a
# Cloudflare deploy needs zero dashboard configuration; `next build` (which
# `opennextjs-cloudflare build` runs) loads this automatically whenever
# NODE_ENV=production, which it always is for a deploy/preview build.
NEXT_PUBLIC_API_URL=https://ttapi.sivayaantechnologies.com
